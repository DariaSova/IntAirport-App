%h2 Baggage for passenger with ID: #{params[:id]}
%table{:border => "1", class: "table table-striped"}
  %tbody
    %tr
      %td Baggage ID
    - @result =  get_baggage(params[:id])
    %tr
    - @result.each do |r|
      %td #{r[0]}
